<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#E9ECEF">

    <!-- Top Bar Section with Blue Header -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="#007BFF"
        android:padding="15dp"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Volunteer"
            android:textColor="#FFFFFF"
            android:textStyle="bold"
            android:textSize="22sp" />
    </LinearLayout>

    <!-- Search Bar Section -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="16dp"
        android:gravity="center_vertical"
        android:background="#FFFFFF">

        <EditText
            android:id="@+id/search_bar"
            android:layout_width="0dp"
            android:layout_height="48dp"
            android:layout_weight="1"
            android:background="@drawable/edit_text_background"
            android:hint="Search"
            android:padding="12dp"
            android:drawablePadding="8dp"
            android:textSize="14sp"
            android:textColorHint="@android:color/darker_gray" />
    </LinearLayout>

    <!-- RecyclerView for NGO Cards -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerViewVolunteer"
        android:layout_width="match_parent"
        android:layout_height="203dp"
        android:layout_weight="1"
        android:clipToPadding="false"
        android:padding="16dp"
        android:scrollbars="vertical" />

    <!-- Button to Open the Form Fragment -->
    <Button
        android:id="@+id/openFormButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="16dp"
        android:backgroundTint="#4CAF50"
        android:text="Fill up form to find more opportunities!"
        android:textColor="#FFFFFF"
        android:textSize="18sp" />

</LinearLayout>



